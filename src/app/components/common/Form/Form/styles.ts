import { css } from 'styled-components';
import { borderGrey } from '@appConstants/colors';

export const container = css`
  border: 1px solid ${borderGrey};
  border-radius: 4px;
  display: grid;
`;
