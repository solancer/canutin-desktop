import styled from 'styled-components';

import { CustomSelect } from '@components/common/Form/Select';
import { container } from './styles';

const ButtonSelect = styled(CustomSelect)`
  ${container}
`;

export default ButtonSelect;
